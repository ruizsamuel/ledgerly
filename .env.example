CLIENT_URL=http://localhost:4200
SERVER_PROXY_TARGET=http://server:5000

DOMAIN=http://localhost # Only for production set up (single domain)

JWT_SECRET=your_jwt_secret_key

# MongoDB credentials (only required for production with docker-compose.prod.yml)
MONGO_USERNAME=admin
MONGO_PASSWORD=adminpassword

LOCALE=en